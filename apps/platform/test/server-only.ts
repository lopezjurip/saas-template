// Test stub for the `server-only` package, which throws when imported outside a
// React Server Component. Vitest aliases `server-only` to this empty module
// (see vitest.config.ts) so server-only modules can be imported under test.
export {};
