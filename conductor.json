{
  "scripts": {
    "setup": "WORKTREE_NAME=$CONDUCTOR_WORKSPACE_NAME WORKTREE_PORT=$CONDUCTOR_PORT WORKTREE_ROOT_PATH=$CONDUCTOR_ROOT_PATH WORKTREE_PROJECT=resolvecom bash scripts/development/worktree-setup.sh",
    "run": "pnpm run -w dev --filter={apps/platform} -- --port $CONDUCTOR_PORT",
    "archive": "WORKTREE_NAME=$CONDUCTOR_WORKSPACE_NAME WORKTREE_PROJECT=resolvecom bash scripts/development/worktree-archive.sh"
  },
  "runScriptMode": "concurrent"
}
